{
  "name": "mock-stripe-server",
  "version": "0.2.2",
  "description": "Mock Stripe API Server",
  "main": "./lib/stripeServer.js",
  "repository": "git@github.com:mdbarr/mock-stripe-server.git",
  "author": "Mark Barr <mark@hyperingenuity.com>",
  "license": "MIT",
  "bin": {
    "mock-stripe-server": "./cli.js"
  },
  "dependencies": {
    "async": "^3.2.0",
    "request": "^2.88.2",
    "restify": "^8.5.1",
    "yargs": "^15.4.1"
  },
  "devDependencies": {
    "eslint": "^7.4.0",
    "mocha": "^8.0.1",
    "stripe": "^8.71.0"
  },
  "scripts": {
    "build": "docker build -t mock-stripe .",
    "docker": "docker run -d --rm --name mock-stripe -p 5757:5757 mock-stripe",
    "lint": "eslint data lib test",
    "start": "forever ./index.js",
    "test": "mocha ./test/testCore.js ./test/*Spec.js"
  }
}
